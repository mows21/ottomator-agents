claude-agent-sdk>=0.1.0
anthropic>=0.40.0
httpx>=0.27.0
python-dotenv>=1.0.0
rich>=13.0.0
asyncio>=3.4.3

# Optional integrations
stripe>=7.0.0
airtable-python-wrapper>=0.15.0

# Document processing (if using document analyzer)
docling>=2.0.0

# Web scraping (if using scraping service)
# crawl4ai>=0.1.0  # Uncomment if needed
