# Multi-Platform Agent Framework Requirements
# ============================================

# Core dependencies
pydantic>=2.10.0
python-dotenv>=1.0.0

# Logging and observability
langfuse>=3.7.0

# HTTP clients
httpx>=0.28.0
aiohttp>=3.11.0

# Pydantic AI Platform
pydantic-ai>=0.0.41

# Claude SDK Platform
anthropic>=0.49.0

# Google ADK Platform
google-generativeai>=0.8.0

# n8n Platform (API client only - n8n itself is separate)
# httpx already included above

# Vector databases and embeddings (optional)
# supabase>=2.13.0

# Sentence transformers for local embeddings (optional)
# sentence-transformers>=2.2.0

# Voyage AI embeddings (optional)
# voyageai>=0.2.0

# OpenAI for embeddings and compatibility
openai>=1.65.0

# Async utilities
asyncio-throttle>=1.0.2

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Type checking
mypy>=1.0.0
